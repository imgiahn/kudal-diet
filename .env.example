# docker-compose가 자동으로 읽는 루트 .env
# cp .env.example .env 후 값 채우기

# ── PostgreSQL ─────────────────────────────────────────
POSTGRES_DB=kudal_db
POSTGRES_USER=kudal
POSTGRES_PASSWORD=your_password_here
