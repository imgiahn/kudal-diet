# ── Web Framework ──────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1

# ── Database (async) ───────────────────────────────────────
sqlalchemy[asyncio]==2.0.31
asyncpg==0.29.0             # PostgreSQL async 드라이버
alembic==1.13.2             # 마이그레이션

# ── Config & Validation ────────────────────────────────────
pydantic==2.8.2
pydantic-settings==2.3.4
python-dotenv==1.0.1

# ── Utilities ──────────────────────────────────────────────
python-multipart==0.0.9     # 파일 업로드 (이미지 분석용)
httpx==0.27.0               # 비동기 HTTP (OpenAI 호출용)
boto3==1.35.0               # Cloudflare R2 업로드 (S3 호환)
openai>=1.30,<2.0           # OpenAI Vision 분석
PyJWT==2.8.0                # JWT 발급/검증
cryptography==43.0.0        # JWT HS256 서명
